function val = L21_norm(U)
% =========================================================================
% 计算 L2,1 范数 (行稀疏)
% U: 输入矩阵
% =========================================================================
% 逐行计算L2范数，然后求和
val = sum(sqrt(sum(U.^2, 2) + eps)); % 添加 eps 增加数值稳定性
end

